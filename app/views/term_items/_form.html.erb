<%= form_for(@term_item) do |f| %>
  <% if @term_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@term_item.errors.count, "error") %> prohibited this term_item from being saved:</h2>

      <ul>
      <% @term_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dataset_id %><br />
    <%= f.number_field :dataset_id %>
  </div>
  <div class="field">
    <%= f.label :term_id %><br />
    <%= f.number_field :term_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
